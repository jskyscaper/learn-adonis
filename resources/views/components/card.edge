@let (className = "rounded-lg overflow-hidden border border-slate-200/60 bg-white text-slate-700 shadow-sm")

<div {{$props.merge({class: [className]}).except(['movi']).toAttrs() }}>
    @if($slots.picture)
    {{{ await $slots.picture() }}}
    @endif

    <div class="p-4">
       {{{ await $slots.main() }}}
    </div>
</div>