@layout() 
    <h1>Writer {{ writer.fullName}}</h1>

    <div class="flex flex-wrap -mx-6">
        @each(movi in writer.moviesWriter)
      <div class="w-full lg:w-1/3 px-3">
        @!movie.card({movi, class : 'w-full'})
      </div>
        
        @end
    </div>
@end